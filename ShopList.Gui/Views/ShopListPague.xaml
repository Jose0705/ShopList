<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ShopList.Gui.Views.NewPage1"
             xmlns:viemodel="clr-namespace:ShopList.Gui.ViewModels"
             x:DataType="viemodel:ShopListViewModels"
             Title="Lista de Compras">
    <Grid
        Padding="8"
        RowSpacing="6"
        RowDefinitions="Auto,*"
        >

        <Grid
            RowSpacing="4"
            ColumnSpacing="4"
            Grid.Row="0"
            Grid.Column="0"
            RowDefinitions="Auto,Auto"
            ColumnDefinitions="Auto,*,Auto,Auto"
            >
            <Label
                Grid.Row="0"
                Grid.Column="0"
                VerticalOptions="Center"
                HorizontalOptions="End"
                FontSize="Medium"
                Text="Producto: "/>
            <Entry
                Grid.Column="1"
                FontSize="Medium"
                Grid.Row="0"
                BackgroundColor="Beige"
                />
            <Label
                FontSize="Medium"
                VerticalOptions="Center"
                HorizontalOptions="End"
                Grid.Row="1"
                Grid.Column="0"
                Text="Cantidad: "/>
            <Entry
                Grid.Column="1"
                Grid.Row="1"
                FontSize="Medium"
                BackgroundColor="Beige"
            />
            <Button
                Grid.Column="2"
                Grid.Row="0"
                FontSize="Medium"
                Text="+"/>
            <Button
                Grid.Column="2"
                FontSize="Medium"
                Grid.Row="1"
                Text="x"/>
        </Grid>

        <CollectionView
            Grid.Row="1"
            Grid.Column="0"
            ItemsSource="{Binding Path=Item}"
            EmptyView="Aun no se ah registrado prodctos"
            BackgroundColor="Beige">
            
        </CollectionView>
        
    </Grid>
</ContentPage>